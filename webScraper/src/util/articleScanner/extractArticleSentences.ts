import Referencer from "../../model/ReferencerModel";

export default function extractArticleSentences(referencerArray: Referencer[]) {
    referencerArray.forEach(referencer => {
        referencer.articlesCompanyReferencedIn
    })
}